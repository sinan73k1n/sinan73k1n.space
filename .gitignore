# ⛔ Kişisel arşiv — ASLA push edilmez
Arşiv/
_arsiv/

# Sırlar
appsettings.Production.json
appsettings.Local.json
*.env
.env
*.key
*.pfx
secrets.json
# Sunucu adresi/izin listesi içerir. Küresel ignore zaten kapsıyor ama o dosya
# depoda değil — koruma klonla birlikte taşınsın diye burada da duruyor.
.claude/settings.local.json

# .NET
bin/
obj/
publish/
*.user
*.suo
.vs/
TestResults/

# Admin'den yüklenen görseller — kullanıcı içeriği, sunucuda yaşar
# (deploy'da rsync --delete YOK kuralı bu yüzden ayrıca önemli)
src/Portfolio.SITE_UI/wwwroot/uploads/

# İçerik deposu çalışma zamanı yedeği (JsonContentStore atomik yazma üretir)
*.json.bak

# Playwright / geçici görsel doğrulama çıktıları
ref-*.png
shot-*.png
node_modules/

# macOS
.DS_Store
