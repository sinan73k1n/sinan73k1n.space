# ⛔ Kişisel arşiv — ASLA push edilmez
Arşiv/
_arsiv/

# Sırlar
appsettings.Production.json
appsettings.Local.json
*.env
.env
*.key
*.pfx
secrets.json
# ⛔ Claude Code çalışma alanı — TAMAMI depo dışı. Kanca betikleri yerel ikinci
# beyin kasasının yolunu taşıyor, ayarlar dosyası da sunucu adresi/izin listesi
# içeriyor. Başka bir makinede işe yaramazlar; herkese açık depoda ise kişisel
# çalışma düzenini ele veriyorlar.
.claude/

# Projenin bana özel çalışma brifingi: yerel not klasörümün yolunu ve sunucu
# notlarını taşıyor. Sürümlemesi özel uzakta (gitea) yapılır, GitHub'da durmaz.
CLAUDE.md

# Geliştirme içeriği: ilk çalıştırmada SeedIcerik'ten ÜRETİLİR, sonra yerel
# düzenlemeler burada birikir. Kişiseldir, repoya girmez. (Production'daki
# karşılığı zaten publish DIŞINDA: ~/portfolio-data/content.json)
src/Portfolio.SITE_UI/App_Data/

# .NET
bin/
obj/
publish/
*.user
*.suo
.vs/
TestResults/

# Admin'den yüklenen görseller — kullanıcı içeriği, sunucuda yaşar
# (deploy'da rsync --delete YOK kuralı bu yüzden ayrıca önemli)
src/Portfolio.SITE_UI/wwwroot/uploads/

# İçerik deposu çalışma zamanı yedeği (JsonContentStore atomik yazma üretir)
*.json.bak

# Playwright / geçici görsel doğrulama çıktıları
ref-*.png
shot-*.png
node_modules/

# macOS
.DS_Store
